<h1><?= $article->getTitle() ?></h1>
<time> <?= $article->getFormattedCreatedAt() ?></time>

<button class="btn-save" data-article-id="<?= $id ?>">Enregistrer</button>

<form action="article.php?id=<?= constructUrl('article', ['id' => $article->getIdArticle()]) ?>" method="POST">
    <label for="author">Pseudo :</label>
    <input type="text" name="author" id="author" value="<?php if (isset($author)) echo $author ?>">

    <label for="comment">Commentaire :</label>
    <textarea name="comment" id="comment" cols="30" rows="10"></textarea>
    <button type="submit">Envoyer</button>
</form>

<h2>Commentaires :</h2>

<?php foreach ($comments as $com) : ?>
    <h3><?= $com['author'] ?></h3>
    <time><?= $com['created_at'] ?></time>
    <p><?= $com['content'] ?></p>
<?php endforeach; ?>