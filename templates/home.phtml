
<section class="columns-container">
    <div class="wrapper">
        <div class="col-1">
            <div class="content-column">
                <?php foreach ($articles as $article) : ?>
                    <article class="standard-article">
                        <div class="img-article" style="background-image: url('<?= $article->getImage(); ?>')">
                            <div class="category-article">
                                <div>
                                    <img src=" <?= $article->getCategoryIcon()?>" alt="">
                                    <a><?= $article->getCategoryName() ?></a>
                                </div>
                                <time><?= $article->getFormattedCreatedAt(); ?></time>
                            </div>
                        </div>

                        <div class="content-article">
                            <h2><?= $article->getTitle(); ?></h2>
                            <p><?= $article->getSummary(); ?></p>
                            <span>
                                <p>by <?= getUserName(constructUrl('article', ['id' => $article->getIdArticle()])) ?></p>
                                <a href="<?= constructUrl('article', ['id' => $article->getIdArticle()]); ?>">Lire la suite</a>
                                <p> <?= constructUrl('article', ['id' => $article->getIdArticle()]); ?> </p>
                            </span>
                        </div>
                    </article>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="col-1">
            <div class="content-column">
            <?php foreach ($articles as $article) : ?>
                    <article class="standard-article">
                        <div class="img-article" style="background-image: url('<?= $article->getImage(); ?>')">
                            <div class="category-article">
                                <div>
                                    <img src=" <?= $article->getCategoryIcon()?>" alt="">
                                    <a><?= $article->getCategoryName() ?></a>
                                </div>
                                <time><?= $article->getFormattedCreatedAt(); ?></time>
                            </div>
                        </div>

                        <div class="content-article">
                            <h2><?= $article->getTitle(); ?></h2>
                            <p><?= $article->getSummary(); ?></p>
                            <span>
                                <p>by <?= getUserName(constructUrl('article', ['id' => $article->getIdArticle()])) ?></p>
                                <a href="<?= constructUrl('article', ['id' => $article->getIdArticle()]); ?>">Lire la suite</a>
                            </span>
                        </div>
                    </article>
                <?php endforeach; ?>
            </div>
        </div>
    </div>

    <div id="right-block">
        <div id="quotes">
            <span>
                <h2>Sci-Fact du Jour</h2>
            </span>
            <p><?= getDayFact() ?></p>
        </div>

        <div id="top-article-box">
            <?php foreach ($topArticles as $topArticle) : ?>
                <article>
                    <div id="img-top" style="background-image: url('<?= $topArticle['image'] ?>')">
                    </div>

                    <h2><?= $topArticle['title'] ?></h2>
                    <p><?= $topArticle['summary'] ?></p>
                </article>
            <?php endforeach; ?>
        </div>
    </div>

</section>